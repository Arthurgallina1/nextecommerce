import { render, screen } from '@testing-library/react'

import Footer from '.'

describe('Footer', () => {
  it('should render 4 columns topics', () => {
    //contact, follow us, link location
  })
})
